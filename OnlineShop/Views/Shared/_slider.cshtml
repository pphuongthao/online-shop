@model List<Model.EF.Slide>

<ol class="carousel-indicators"> 
    @for (var j = 0; j < Model.Count; ++j)
    {
        if (j == 0)
        {
            <li data-target="#carouselExampleIndicators" data-slide-to="@j" class="active"></li>
        }
        else
        {
            <li data-target="#carouselExampleIndicators" data-slide-to="@j"></li>
        }
    }
</ol>
<div class="carousel-inner">
    @{ int i = 0;}
    @foreach (var item in Model)
    {
        i++;
        var active = i == 1 ? "active" : "";
        var itemCSS = "item" + i;
        <div class="carousel-item @itemCSS @active" style="background-image:url('@item.Image')">
            <div class="container">
                <div class="w3l-space-banner">
                    <div class="carousel-caption p-lg-5 p-sm-4 p-3">
                        <p>
                            @Html.Raw(item.Title)
                        </p>
                        <h3 class="font-weight-bold pt-2 pb-lg-5 pb-4">
                            @Html.Raw(item.Description)
                        </h3>
                        <a class="button2" href="@item.Link">Shop Now </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
<a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
</a>
<a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
</a>